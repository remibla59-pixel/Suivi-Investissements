// --- VUE : VALORISATIONS D'UN COMPTE ---
// Détail d'un compte : valorisations, allocation, évolution, mouvements (extraite du fichier principal, axe 2 de l'audit).
import { ArrowLeft, Edit2, Trash2, TrendingUp, History, PlusCircle } from "lucide-react";
import { AreaChart, Area, CartesianGrid, XAxis, YAxis, Tooltip, ResponsiveContainer } from "recharts";
import { INVESTMENT_CATEGORIES, CURRENCIES } from "../../utils/constantes.js";
import { calculateXIRR, getNetInvestedUntilDate, calculateTWRFromSnapshots, formatCompactAxis } from "../../utils/calculs.js";
import { BlurMoney, PerformanceBadge, ChartTooltip, ChartLegend, axisTickProps, gridStroke } from "../ui.jsx";

export const SnapshotsView = ({ selectedBroker, selectedAccount, privacyMode, darkMode, getAccountCurrentValueRaw, getAccountInvestedTotalRaw, getLatestSnapshot, openModal, onDeleteSnapshot, onBack }) => {
    const snapshots = selectedAccount.snapshots || []; 
    const currentVal = getAccountCurrentValueRaw(selectedAccount); 
    const investedVal = getAccountInvestedTotalRaw(selectedAccount); 
    const lastSnap = getLatestSnapshot(selectedAccount); 
    
    const chartData = snapshots.map(s => ({ 
        date: new Date(s.date).toLocaleDateString('fr-FR', {month:'short', year:'2-digit'}), 
        val: parseFloat(s.amount),
        invested: getNetInvestedUntilDate(selectedAccount.movements || [], s.date)
    })); 
    
    const dist = lastSnap?.categories?.map(c => ({ ...c, ...(INVESTMENT_CATEGORIES.find(i => i.value === c.type)) })).sort((a, b) => parseFloat(b.amount) - parseFloat(a.amount)) || []; 
    const currency = selectedAccount.currency || 'EUR'; 
    const symbol = CURRENCIES.find(c => c.code === currency)?.symbol || '€'; 
    const tri = calculateXIRR(selectedAccount.movements, currentVal);
    const twrInfo = calculateTWRFromSnapshots(snapshots, selectedAccount.movements);

    return (<div className="space-y-6 animate-fade-in w-full"><div className="flex items-center gap-4"><button onClick={() => { onBack(); }} className="p-2 hover:bg-white dark:hover:bg-slate-800 rounded-lg border border-gray-200 dark:border-slate-700 text-gray-600 dark:text-gray-300"><ArrowLeft className="w-6 h-6" /></button><div><div className="flex items-center gap-2"><h2 className="text-2xl font-bold text-gray-800 dark:text-white">{selectedAccount.name}</h2>{currency !== 'EUR' && <span className="px-2 py-0.5 bg-orange-100 dark:bg-orange-900/30 text-orange-800 dark:text-orange-300 text-xs font-bold rounded">{currency}</span>}</div><p className="text-gray-500 dark:text-gray-400">{selectedBroker.name}</p></div></div><div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4"><div className="bg-white dark:bg-slate-800 p-4 rounded-xl border border-gray-100 dark:border-slate-700 shadow-lg"><div className="text-sm text-gray-500 dark:text-gray-400 mb-1">Valeur Actuelle</div><div className="text-2xl font-bold text-gray-900 dark:text-white"><BlurMoney amount={currentVal} currency={symbol} privacyMode={privacyMode} /></div></div><div className="bg-white dark:bg-slate-800 p-4 rounded-xl border border-gray-100 dark:border-slate-700 shadow-lg cursor-pointer hover:border-blue-300 dark:hover:border-blue-500 group" onClick={() => openModal('movementList')}><div className="text-sm text-gray-500 dark:text-gray-400 mb-1 flex items-center justify-between">Capital Investi <Edit2 className="w-3 h-3 text-gray-300 group-hover:text-blue-500 transition-colors" /></div><div className="text-2xl font-bold text-gray-900 dark:text-white"><BlurMoney amount={investedVal} currency={symbol} privacyMode={privacyMode} /></div></div><div className="bg-white dark:bg-slate-800 p-4 rounded-xl border border-gray-100 dark:border-slate-700 shadow-lg"><div className="text-sm text-gray-500 dark:text-gray-400 mb-1">Plus/Moins Value</div><div className="text-2xl font-bold text-gray-900 dark:text-white">{investedVal > 0 ? (currentVal - investedVal > 0 ? '+' : '') : ''} <BlurMoney amount={investedVal > 0 ? (currentVal - investedVal) : 0} currency={symbol} privacyMode={privacyMode} /></div></div><div className="bg-white dark:bg-slate-800 p-4 rounded-xl border border-gray-100 dark:border-slate-700 shadow-lg"><div className="text-sm text-gray-500 dark:text-gray-400 mb-1">Performance</div><div className="text-2xl font-bold flex items-center"><PerformanceBadge current={currentVal} invested={investedVal} tri={tri} twr={twrInfo ? twrInfo.annualized : null} /></div></div></div><div className="grid grid-cols-1 lg:grid-cols-3 gap-6"><div className="lg:col-span-1 bg-white dark:bg-slate-800 p-6 rounded-xl border border-gray-100 dark:border-slate-700 shadow-lg h-fit"><h3 className="font-bold mb-4 text-gray-800 dark:text-white">Allocation</h3>{dist.length > 0 ? (<div className="space-y-3 pt-2">{dist.map((d, i) => (<div key={i} className="flex justify-between items-center text-sm"><span className="flex items-center text-gray-600 dark:text-gray-300 font-medium"><div className="w-2 h-2 rounded-full mr-3" style={{backgroundColor: d.color}}></div>{d.label}</span><span className="font-bold text-gray-800 dark:text-white"><BlurMoney amount={parseFloat(d.amount)} currency={symbol} privacyMode={privacyMode} /></span></div>))}</div>) : <p className="text-gray-400 text-sm">Pas de données</p>}</div><div className="lg:col-span-2 bg-white dark:bg-slate-800 p-6 rounded-xl border border-gray-100 dark:border-slate-700 shadow-lg"><div className="flex flex-wrap items-center justify-between gap-2 mb-4"><h3 className="font-bold text-gray-800 dark:text-white flex items-center gap-2"><TrendingUp className="w-5 h-5 text-blue-500" /> Évolution ({currency})</h3><ChartLegend items={[{ name: 'Valeur', color: '#2563EB' }, { name: 'Investi', color: '#10B981' }]} /></div><div className="h-72">{chartData.length > 1 ? (<ResponsiveContainer width="100%" height="100%"><AreaChart data={chartData} margin={{ top: 8, right: 10, left: 0, bottom: 0 }}><defs><linearGradient id="gradVal" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#2563EB" stopOpacity={0.2} /><stop offset="100%" stopColor="#2563EB" stopOpacity={0} /></linearGradient><linearGradient id="gradInv" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#10B981" stopOpacity={0.12} /><stop offset="100%" stopColor="#10B981" stopOpacity={0} /></linearGradient></defs><CartesianGrid {...gridStroke(darkMode)} /><XAxis dataKey="date" tick={{ ...axisTickProps(darkMode) }} axisLine={false} tickLine={false} tickMargin={6} minTickGap={20} /><YAxis width={54} domain={['dataMin', 'dataMax']} tick={{ ...axisTickProps(darkMode) }} axisLine={false} tickLine={false} tickFormatter={v => privacyMode ? '***' : formatCompactAxis(v)} /><Tooltip content={<ChartTooltip darkMode={darkMode} formatter={(value) => privacyMode ? '****' : new Intl.NumberFormat('fr-FR', { style: 'currency', currency: 'EUR' }).format(value)} />} /><Area isAnimationActive={false} name="Valeur" type="monotone" dataKey="val" stroke="#2563EB" strokeWidth={2.5} fill="url(#gradVal)" fillOpacity={1} dot={{ r: 2.5, fill: '#2563EB', strokeWidth: 0 }} activeDot={{ r: 5 }} /><Area isAnimationActive={false} name="Investi" type="monotone" dataKey="invested" stroke="#10B981" strokeWidth={2} strokeDasharray="5 5" fill="url(#gradInv)" fillOpacity={1} dot={false} /></AreaChart></ResponsiveContainer>) : <div className="h-full flex items-center justify-center text-gray-400 bg-gray-50 dark:bg-slate-700 rounded-lg">Ajoutez au moins 2 valorisations</div>}</div></div></div><div className="flex gap-4 mt-8"><button onClick={() => openModal('movementList')} className="flex-1 bg-indigo-50 dark:bg-indigo-900/20 border border-indigo-200 dark:border-indigo-800 text-indigo-700 dark:text-indigo-300 px-4 py-3 rounded-xl hover:bg-indigo-100 dark:hover:bg-indigo-900/30 font-bold flex items-center justify-center gap-2 shadow-sm transition-all"><History className="w-5 h-5" /> Mouvements</button><button onClick={() => openModal('snapshot')} className="flex-1 bg-blue-600 text-white px-4 py-3 rounded-xl hover:bg-blue-700 shadow-lg font-bold flex items-center justify-center gap-2 transition-all"><PlusCircle className="w-5 h-5" /> Nouvelle Valorisation</button></div><div className="mt-6 space-y-3"><h3 className="text-xl font-bold text-gray-800 dark:text-white mb-4">Historique de valorisation</h3>{[...snapshots].reverse().map((s, i) => { const prev = snapshots[snapshots.length - 2 - i]; const diff = prev ? parseFloat(s.amount) - parseFloat(prev.amount) : 0; return (<div key={s.id} className="bg-white dark:bg-slate-800 p-4 rounded-xl border border-gray-100 dark:border-slate-700 flex justify-between items-center hover:shadow-lg transition-all"><div><div className="font-bold text-gray-800 dark:text-white">{new Date(s.date).toLocaleDateString('fr-FR', {day:'numeric', month:'long', year:'numeric'})}</div><div className="text-sm flex items-center mt-1"><span className="font-bold text-gray-700 dark:text-gray-300 mr-3"><BlurMoney amount={parseFloat(s.amount)} currency={symbol} privacyMode={privacyMode} /></span>{prev && <span className={`flex items-center text-xs font-semibold px-2 py-0.5 rounded ${diff >= 0 ? "bg-green-100 text-green-700 dark:bg-green-900/30 dark:text-green-400" : "bg-red-100 text-red-700 dark:bg-red-900/30 dark:text-red-400"}`}>{diff > 0 ? '+' : ''}<BlurMoney amount={diff} currency={symbol} privacyMode={privacyMode} /></span>}</div></div><div className="flex gap-1"><button onClick={() => openModal('snapshot', s)} className="p-2 text-gray-300 hover:text-blue-500 hover:bg-blue-50 dark:hover:bg-blue-900/30 rounded-lg"><Edit2 className="w-5 h-5" /></button><button onClick={() => onDeleteSnapshot(selectedBroker.id, selectedAccount.id, s.id)} className="p-2 text-gray-300 hover:text-red-500 hover:bg-red-50 dark:hover:bg-red-900/30 rounded-lg"><Trash2 className="w-5 h-5" /></button></div></div>); })}</div></div>);
};